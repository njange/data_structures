# FIFO
# Just like a real world queue, queue data structure allows insertion of an element at one end and deletion of an element at the other end.
# This queue is called First In First Out (FIFO) queue because the element which is inserted first is retrieved first.